# ─────────────────────────────────────────────────────────────────────────────
# Pearls AQI Predictor — Lightweight Deploy Dependencies (Render Free Tier)
# Excludes PyTorch/CUDA to stay within 512MB RAM limit
# ─────────────────────────────────────────────────────────────────────────────

# Core
pydantic>=2.0,<3.0
pydantic-settings>=2.0,<3.0
python-dotenv>=1.0

# Data Processing
numpy>=1.24,<2.0
pandas>=2.0,<3.0

# API Clients
aiohttp>=3.8
tenacity>=8.2
requests>=2.28

# Machine Learning (champion model only — memory-constrained free tier)
scikit-learn>=1.3

# Web Framework
flask>=3.0
flask-cors>=4.0
gunicorn>=21.0

# Configuration
pyproject-toml>=0.0.10

# Typing
typing-extensions>=4.6
